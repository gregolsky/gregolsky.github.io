h2 articles

ul.articles
  each article, slug in public.articles._data
    li
      a(href="/articles/#{ slug }")
        .article
          .title #{ article.title }
          .cover
            if article.pic
              img(src="#{ article.pic }")
